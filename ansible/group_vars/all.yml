---
# Server IPs - Update these when IPs change
server_ips:
  ansible-control: "10.0.1.10"
  app-server: "10.0.1.20"
  nexus-server: "10.0.1.30"
  jenkins-master: "10.0.1.40"

# Application settings
app_name: "nodejs-app"
app_port: 3000
app_user: "ec2-user"
app_group: "ec2-user"

# Nexus settings
nexus_port: 8081
nexus_repo: "node-app-releases"

# Jenkins settings
jenkins_port: 8080